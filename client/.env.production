# Worker serves the client at the same origin, so relative /api and same-host
# /ws URLs work without override. Set these if you ever deploy the API and
# the static client on separate origins.
# VITE_API_URL=https://trek-api.<account>.workers.dev
# VITE_WS_URL=wss://trek-api.<account>.workers.dev/ws
